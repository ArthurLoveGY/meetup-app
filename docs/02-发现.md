# Tab 2：发现 — 功能评估文档

> 路径：`pages/discover/index` | 关联页面：1 个
> 评估日期：2026-06-12

---

## 功能架构

```
发现主页 (discover)
└── 点击行程 → 行程详情 (trip-detail)  [复用行程圈]
```

---

## 1. 发现主页

**文件**：`src/pages/discover/index.tsx` (56 行)

### 功能清单

| 功能 | 状态 | 实现细节 |
|------|------|---------|
| 行程列表 | ✅ | 复用 `TripCard` 组件 |
| 点击跳转 | ✅ | `navigateTo trip-detail` |
| 加载态 | ✅ | `LoadingView` |
| 空状态 | ✅ | `EmptyState` 组件 |
| 搜索 | ❌ | 无搜索框 |
| 分类筛选 | ❌ | 无分类 Tab |
| 地图模式 | ❌ | 无地理位置发现 |
| 推荐算法 | ❌ | 直接复用 `useTripStore.trips` |
| 热门排行 | ❌ | 无排序逻辑 |
| 附近的人 | ❌ | 未实现 |

### 数据流

```
useLoad → useTripStore.fetchTrips(true)
    → tripService.getTrips(1, 10)  // 与主页完全相同的 API
        → GET /trips?page=1&pageSize=10
```

### 依赖关系

- **Store**：`useTripStore`（与主页共享同一个 store 实例）
- **Service**：`tripService.getTrips`
- **组件**：`TripCard`, `EmptyState`, `LoadingView`
- **平台**：`platformService.navigateTo`

### 与主页对比

| 维度 | 主页（行程圈） | 发现页 |
|------|--------------|--------|
| 数据源 | `useTripStore.trips` | `useTripStore.trips`（同一份数据） |
| API | `GET /trips` | `GET /trips`（完全相同） |
| 分页 | ✅ 有 | ❌ 无 |
| FAB 按钮 | ✅ 有 | ❌ 无 |
| 下拉刷新 | ✅ 有 | ❌ 无 |
| 行程卡片 | ✅ 完整信息 | ✅ 完整信息 |

**结论：发现页与主页的数据源、API、UI 组件完全相同，仅少了分页和 FAB 按钮。**

---

## 问题汇总

| # | 严重度 | 问题 | 影响 |
|---|--------|------|------|
| 1 | 🔴 高 | 与主页完全重复，无差异化内容 | Tab 存在意义不明 |
| 2 | 🔴 高 | 无搜索功能 | 用户无法主动发现内容 |
| 3 | 🔴 高 | 无分类筛选 | 无法按聚餐/旅行/运动等筛选 |
| 4 | 🟡 中 | 无推荐算法 | 无法个性化推荐 |
| 5 | 🟡 中 | 无附近/热门发现 | 缺少核心发现场景 |
| 6 | 🟡 中 | 无分页/刷新 | 交互体验不完整 |

---

## 改进建议

### 方案 A：搜索+分类发现

```
发现页
├── 搜索栏（搜索行程标题/地点/标签）
├── 分类 Tab（全部/聚餐/旅行/运动/看展/游戏/学习）
├── 热门标签
└── 行程列表（支持排序：最新/最热/附近）
```

**需要后端支持**：
- `GET /trips/search?keyword=xxx`
- `GET /trips?type=meal&sort=popular`
- `GET /trips/nearby?lat=xxx&lng=xxx`

### 方案 B：社交发现

```
发现页
├── 好友在参加的行程
├── 你可能感兴趣的行程（基于兴趣标签）
├── 热门行程排行
└── 附近的公开行程
```

### 方案 C：合并到主页

如果发现功能短期不扩展，建议将此 Tab 合并到主页（行程圈），改为：
- 主页增加搜索栏
- 主页增加分类筛选
- 底部 Tab 从 5 个减为 4 个

---

## 发现总结

### 完成度统计

| 功能点 | 已完成 | 完成率 |
|--------|--------|--------|
| 6 | 3 | 50% |

**核心问题：发现页功能与主页完全重复，需要明确差异化定位。**
